coverage:
  status:
    patch:
      default:
        threshold: "5%"
    project:
      default:
        threshold: "0.1%"

  ignore:
    - .github/.*
    - app/assets/.*
    - app/views/.*
    - public/.*
    - spec/.*
    - vendor/.*
